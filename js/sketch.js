

function setup() {
    var canvas = createCanvas (800, 800);
    canvas.parent("p5container");
    background(200);
    for(i=0; i<25; i++){
     var circle0bj = {
        x: random(width),
        y: random(height),
        r: 30
    } circleArr.push(circle0bj)
        
   for(var j =0; j<circleArrat. length; j++){

        ellipse(circleArr[j].x, circleArr[j].y, circleArr[j].r)
    }
    }


}
function draw() {
    
}